#include "lists.h"

/**
 * listint_len - measures the length of the list
 * @h: the list to measure
 * Return: the length of the list
 */

size_t listint_len(const listint_t *h)
{
	size_t count = 0;

	while (h != NULL)
	{
		h = h->next;
		count++;
	}
	return (count);
}
